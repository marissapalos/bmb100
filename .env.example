# Bulldog Marching Band Centennial — environment placeholders
#
# Copy this file to `.env.local` (which is git-ignored) and fill in values
# as features are wired up. Nothing here is used yet — this is v2 scaffolding
# for the eventual Supabase-backed reunion registration / RSVP flow.

# Supabase (v2 — not yet integrated)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
